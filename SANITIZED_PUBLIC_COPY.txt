Sanitized public copy prepared on 2026-03-18.

What was removed or cleaned before publication:

- private keys
- wallet addresses
- API keys
- proxy credentials
- local HTML snapshots
- Python cache files
- secret values in `.env`

What remains intentionally:

- source code
- public chain contract addresses
- public token addresses used by the bot
- empty template files for local setup

Before using this copy:

1. Fill your own `.env`
2. Fill `wallets.txt`, `keys.txt`, `api_keys.txt`, `proxies.txt`
3. Keep files aligned line-by-line
4. Test in safe mode first
